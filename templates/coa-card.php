<?php
/**
 * COA card template.
 *
 * This file is intentionally thin in v1.0.0. Public output is centralized in
 * COAM_Renderer::render_card() so shortcode, PHP API, and WooCommerce output
 * share the same escaped markup.
 *
 * @package COAM
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
